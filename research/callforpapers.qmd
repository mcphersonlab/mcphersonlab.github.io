<!-- 
This file contains the call for papers table for the research page.
To edit call for papers information:
1. Update call metadata in `_callforpapers_data.py`
2. Add, remove, or modify entries following the existing format
3. Each entry needs: journal, impact, due_date, topic, and website
   - due_date may be YYYY-MM-DD, TBD, or Ongoing
4. The 'impact' field should contain the Journal Impact Factor (JIF) from JCR/Clarivate
   - Use empty string ("") if impact factor is unavailable
    - For multi-journal calls with separate submissions to each journal, separate values with " / " (e.g., "19.4 / 87.3")
    - For collection calls spanning multiple journals, use the primary or most representative journal's impact factor
5. Project links for active projects are derived from `research/projects.qmd`'s Current Active Projects table and may be overwritten during sync
6. The table will automatically update based on this data

This file is included in research/index.qmd and renders on the research page.
-->

## Calls for Papers

```{python}
#| echo: false
#| warning: false

import pandas as pd
from datetime import datetime, timedelta
from IPython.display import display, HTML
import json


# PROJECTS in need of a (home) call for paper
# "https://github.com/mcphersonlab/cost-amr", 
# "https://github.com/mcphersonlab/roi-pipeline-antimicrobial"
# "https://github.com/mcphersonlab/biophysics-proteins-as-complex-machines-of-physics"
# "https://github.com/mcphersonlab/mgx-mbx-colorectal-cancer"
# "https://github.com/mcphersonlab/ai-microbiome-transmission",
# "https://github.com/mcphersonlab/structure-function-pharmacology"


# Call for papers data is imported from _callforpapers_data.py
from _callforpapers_data import (
    call_for_papers,
    SPECIAL_CALL_DUE_DATES,
    SPECIAL_CALL_DUE_DATE_RULES,
)


def format_call_due_date(due_date, journal):
    if due_date in SPECIAL_CALL_DUE_DATE_RULES:
        return SPECIAL_CALL_DUE_DATE_RULES[due_date]['display_date']
    try:
        return datetime.strptime(due_date, '%Y-%m-%d').strftime('%B %d, %Y')
    except ValueError as exc:
        raise ValueError(
            f"Invalid call-for-papers due_date {due_date!r} for journal {journal!r} in the shared call-for-papers data"
        ) from exc


call_for_papers_project_data = [
    {
        "journal": paper["journal"],
        "impact": paper.get("impact", ""),
        "website": paper["website"],
        "due_date": paper["due_date"],
        "display_date": format_call_due_date(paper["due_date"], paper["journal"]),
        "projects": [
            project_url.strip()
            for project_url in (paper.get("projects") or [])
            if isinstance(project_url, str) and project_url.strip()
        ],
    }
    for paper in call_for_papers
]

display(HTML(f"""
<script id="research-index-call-for-papers-project-data" type="application/json">
{json.dumps(call_for_papers_project_data, ensure_ascii=True)}
</script>
"""))
            
def _impact_sort_key(impact_str):
    """Return a float sort key for an impact factor string; missing/unparseable → large sentinel."""
    import re as _re
    if not impact_str:
        return 99999.0
    m = _re.search(r'(\d+\.?\d*)', impact_str)
    return float(m.group(1)) if m else 99999.0


def create_callforpapers_html(papers_data):
    current_date = datetime.now()
    cutoff_date = current_date - timedelta(days=180)

    # Parse dates and sort (most recent first, then by journal name)
    # Convert placeholder deadlines using the shared rules from _callforpapers_data.py
    for paper in papers_data:
        if paper['due_date'] in SPECIAL_CALL_DUE_DATE_RULES:
            rule = SPECIAL_CALL_DUE_DATE_RULES[paper['due_date']]
            paper['sort_date'] = rule['sort_date']
            paper['display_date'] = rule['display_date']
        else:
            paper['sort_date'] = datetime.strptime(paper['due_date'], '%Y-%m-%d')
            paper['display_date'] = paper['sort_date'].strftime('%B %d, %Y')

    # Remove entries more than 6 months past their due date
    filtered_papers = [
        p for p in papers_data
        if p['due_date'] in SPECIAL_CALL_DUE_DATES or p['sort_date'] >= cutoff_date
    ]

    # Sort by date (earliest first), but put "TBD" and "Ongoing" at the end
    sorted_papers = sorted(filtered_papers, key=lambda x: (x['sort_date'], x['journal']))
    
    html = """
    <style>
        .callforpapers-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 14px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .callforpapers-table thead tr {
            background-color: #dc3545;
            color: white;
            text-align: left;
            font-weight: bold;
        }
        .callforpapers-table th,
        .callforpapers-table td {
            padding: 12px 15px;
            border: 1px solid #ddd;
        }
        .callforpapers-table th.cfp-sortable {
            cursor: pointer;
            user-select: none;
            white-space: nowrap;
        }
        .callforpapers-table th.cfp-sortable:hover {
            background-color: #c82333;
        }
        .cfp-sort-indicator {
            display: inline-block;
            margin-left: 4px;
            font-size: 11px;
            opacity: 0.7;
        }
        .callforpapers-table tbody tr {
            border-bottom: 1px solid #dddddd;
        }
        .callforpapers-table tbody tr:nth-of-type(even) {
            background-color: #f3f3f3;
        }
        .callforpapers-table tbody tr:hover {
            background-color: #f8d7da;
            cursor: pointer;
        }
        .callforpapers-table tbody tr.upcoming-deadline {
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
        }
        .callforpapers-table tbody tr.upcoming-deadline:hover {
            background-color: #ffe69c;
        }
        .callforpapers-table tbody tr.past-deadline {
            opacity: 0.45;
        }
        .callforpapers-table tbody tr.no-project {
            background-color: #fce0c8;
            border-left: 4px solid #cc5500;
        }
        .callforpapers-table tbody tr.no-project:hover {
            background-color: #f8c9a8;
        }
    </style>
    <table class="callforpapers-table" id="callforpapers-sortable-table">
        <thead>
            <tr>
                <th>Journal</th>
                <th class="cfp-sortable" data-sort-col="impact" title="Sort by Impact Factor">Impact <span class="cfp-sort-indicator"></span></th>
                <th class="cfp-sortable" data-sort-col="date" title="Sort by Due Date">Due Date <span class="cfp-sort-indicator">▼</span></th>
                <th>Topic</th>
                <th>Project</th>
            </tr>
        </thead>
        <tbody>
    """
    
    for paper in sorted_papers:
        # Determine row classes based on due date status and project presence
        classes = []
        if paper['due_date'] not in SPECIAL_CALL_DUE_DATES:
            days_until = (paper['sort_date'] - current_date).days
            if days_until < 0:
                classes.append("past-deadline")
            elif 0 <= days_until <= 60:
                classes.append("upcoming-deadline")
        
        raw_projects = paper.get('projects')
        if isinstance(raw_projects, (list, tuple)):
            projects = [p.strip() for p in raw_projects if isinstance(p, str) and p.strip()]
        elif isinstance(raw_projects, str):
            projects = [raw_projects.strip()] if raw_projects.strip() else []
        else:
            projects = []
        has_project = bool(projects)
        if not has_project:
            classes.append("no-project")
        
        row_class = f' class="{" ".join(classes)}"' if classes else ""

        # Compute sort keys for data attributes
        date_sort_key = int(paper['sort_date'].strftime('%Y%m%d'))
        impact = paper.get('impact', '')
        impact_sort_key = _impact_sort_key(impact)
        
        website_html = f"<a href='{paper['website']}' target='_blank' rel='noopener noreferrer' style='color: #007bff;'>Link</a>"
        # Format Journal column with journal name and website link below
        journal_cell = f"<strong>{paper['journal']}</strong><br>{website_html}"
        
        # Format Impact column
        impact_cell = f'<span style="font-weight: bold;">{impact}</span>' if impact else '—'
        
        # Add project links - support multiple project-specific links
        if projects:
            project_links = []
            for project_url in projects:
                project_links.append(f"<a href='{project_url}' target='_blank' rel='noopener noreferrer' style='color: #007bff;'>Link</a>")
            project_cell = "<br>".join(project_links)
        else:
            project_cell = ""
        
        html += f"""
            <tr{row_class} data-sort-date="{date_sort_key}" data-sort-impact="{impact_sort_key}">
                <td>{journal_cell}</td>
                <td data-sort-val="{impact_sort_key}">{impact_cell}</td>
                <td data-sort-val="{date_sort_key}">{paper['display_date']}</td>
                <td>{paper['topic']}</td>
                <td>{project_cell}</td>
            </tr>
        """
    
    html += """
        </tbody>
    </table>
    <script>
    (function () {
      var table = document.getElementById('callforpapers-sortable-table');
      if (!table) return;
      var sortState = { col: 'date', asc: true };

      function updateIndicators() {
        table.querySelectorAll('th.cfp-sortable').forEach(function (th) {
          var ind = th.querySelector('.cfp-sort-indicator');
          if (!ind) return;
          if (th.dataset.sortCol === sortState.col) {
            ind.textContent = sortState.asc ? '▼' : '▲';
          } else {
            ind.textContent = '';
          }
        });
      }

      function sortTable(col, asc) {
        var tbody = table.querySelector('tbody');
        if (!tbody) return;
        var rows = Array.from(tbody.rows);
        var attr = col === 'impact' ? 'data-sort-impact' : 'data-sort-date';
        rows.sort(function (a, b) {
          var av = parseFloat(a.getAttribute(attr)) || 0;
          var bv = parseFloat(b.getAttribute(attr)) || 0;
          return asc ? av - bv : bv - av;
        });
        rows.forEach(function (row) { tbody.appendChild(row); });
      }

      table.querySelectorAll('th.cfp-sortable').forEach(function (th) {
        th.addEventListener('click', function () {
          var col = th.dataset.sortCol;
          if (sortState.col === col) {
            sortState.asc = !sortState.asc;
          } else {
            sortState.col = col;
            sortState.asc = col === 'date';
          }
          sortTable(sortState.col, sortState.asc);
          updateIndicators();
        });
      });
    })();
    </script>
    """
    return html

display(HTML(create_callforpapers_html(call_for_papers)))
```

And other great journals across: 

**Science**: 
- [*Nature*](https://www.nature.com/)
- [*Science*](https://www.science.org/)
- [*Cell*](https://www.cell.com/)

**Medicine**: 
- [*The Lancet*](https://www.thelancet.com)
- [*New England Journal of Medicine*](https://www.nejm.org)
- [*Journal of American Medical Association (JAMA)*](https://jamanetwork.com/journals/jama)
- [*Nature Medicine*](https://www.nature.com/nm/)

**Pharmacy**: 
- [*American Journal of Health-System Pharmacy (AJHP)*](https://academic.oup.com/ajhp)
- [*American Journal of Pharmaceutical Education (AJPE)*](https://www.ajpe.org/)

**Pharmacology**
- [*Nature Reviews Drug Discovery*](https://www.nature.com/nrd/)
- [*Pharmacological Reviews*](https://pharmrev.aspetjournals.org/)
- [*Trends in Pharmacological Sciences*](https://www.cell.com/trends/pharmacological-sciences/home)

**Chemistry**: 
- [*JACS*](https://pubs.acs.org/journal/jacsat)
- [*JCTC*](https://pubs.acs.org/journal/jctcce) 
- [*JCIM*](https://pubs.acs.org/journal/jcisd8)
- [*Chemical Reviews*](https://pubs.acs.org/journal/chreay)

**Infectious Diseases**: 
- [*Clinical Infectious Diseases (CID)*](https://academic.oup.com/cid)
- [*Nature Microbiology*](https://www.nature.com/nmicrobiol/)
- [*Nature Reviews Microbiology*](https://www.nature.com/nrmicro/)
- [*The Lancet Infectious Diseases*](https://www.thelancet.com/journals/laninf/home)
- [*The Lancet Microbe*](https://www.thelancet.com/journals/lanmic/home)
- [*The Lancet HIV*](https://www.thelancet.com/journals/lanhiv/home)
- [*Cell Host and Microbe*](https://www.cell.com/cell-host-microbe/home)
- [*Antimicrobial Agents and Chemotherapy (AAC)*](https://journals.asm.org/journal/aac)
- [*Clinical Microbiology Reviews (CMR)*](https://journals.asm.org/journal/cmr)
- [*Journal of Virology*](https://journals.asm.org/journal/jvi)
- [*Clinical Microbiology and Infection (CMI)*](https://www.clinicalmicrobiologyandinfection.org/)
- [*Journal of Antimicrobial Chemotherapy (JAC)*](https://academic.oup.com/jac)
- [*Anaerobe*](https://www.sciencedirect.com/journal/anaerobe)
